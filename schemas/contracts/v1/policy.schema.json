{
  "$comment": "generated from pinned pydantic + type-level overrides, guarded by generate-and-diff (nel-contracts projector 1); a fuller hand-controlled projection layer is deferred (K-phase)",
  "$id": "urn:nel:contracts:v1:policy",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "A neutral policy identity. The real orchestration policy engine (default-deny\nclassification, per-boundary decisions) is kernel-side (K1); this is the shared shape.",
  "properties": {
    "description": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Description"
    },
    "policy_id": {
      "pattern": "^[a-z0-9][a-z0-9-]{0,63}$",
      "title": "Policy Id",
      "type": "string"
    }
  },
  "required": [
    "policy_id"
  ],
  "title": "Policy",
  "type": "object"
}
