{
  "$comment": "generated by nel-contracts projector 1",
  "$defs": {
    "RepoRef": {
      "additionalProperties": false,
      "description": "A logical, portable repository reference. Carries no filesystem path (a public\ncontract must never leak a governor-local ``C:\\Users`` path).",
      "properties": {
        "host": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Host"
        },
        "repo": {
          "pattern": "^[a-z0-9][a-z0-9-]{0,63}$",
          "title": "Repo",
          "type": "string"
        }
      },
      "required": [
        "repo"
      ],
      "title": "RepoRef",
      "type": "object"
    }
  },
  "$id": "urn:nel:contracts:v1:repo-metadata",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "The GitHub-metadata freshness contract (design D10).\n\nFreshness is derived, not stored: ``archived`` wins, otherwise activity older than\n``stale_after_days`` (default 180) is ``stale``, else ``active``.",
  "properties": {
    "archived": {
      "default": false,
      "title": "Archived",
      "type": "boolean"
    },
    "default_branch": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Default Branch"
    },
    "last_activity_at": {
      "format": "date-time",
      "title": "Last Activity At",
      "type": "string"
    },
    "repo": {
      "$ref": "#/$defs/RepoRef"
    },
    "stale_after_days": {
      "default": 180,
      "minimum": 1,
      "title": "Stale After Days",
      "type": "integer"
    },
    "stars": {
      "anyOf": [
        {
          "minimum": 0,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Stars"
    }
  },
  "required": [
    "repo",
    "last_activity_at"
  ],
  "title": "RepoMetadata",
  "type": "object"
}
