{
  "$comment": "generated by nel-contracts projector 1",
  "$defs": {
    "ClaimedActor": {
      "additionalProperties": false,
      "description": "An inbound, self-asserted actor carried on a ``WorkIntent``.\n\nEvery field is a claim. It is NOT an authorization input: the kernel re-derives the real\nactor from the authenticated channel identity (design §4). ``attestation_state`` is pinned\nto ``unattested`` so a public consumer cannot confuse it with a kernel-attested actor.",
      "properties": {
        "actor_kind": {
          "examples": [
            "claude_code",
            "codex",
            "game_engine",
            "github_actions",
            "http",
            "human",
            "mcp",
            "script"
          ],
          "title": "ActorKind",
          "type": "string"
        },
        "attestation_state": {
          "const": "unattested",
          "default": "unattested",
          "title": "Attestation State",
          "type": "string"
        },
        "display_name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Display Name"
        },
        "is_advisory": {
          "default": true,
          "title": "Is Advisory",
          "type": "boolean"
        }
      },
      "required": [
        "actor_kind"
      ],
      "title": "ClaimedActor",
      "type": "object"
    },
    "RepoRef": {
      "additionalProperties": false,
      "description": "A logical, portable repository reference. Carries no filesystem path (a public\ncontract must never leak a governor-local ``C:\\Users`` path).",
      "properties": {
        "host": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Host"
        },
        "repo": {
          "pattern": "^[a-z0-9][a-z0-9-]{0,63}$",
          "title": "Repo",
          "type": "string"
        }
      },
      "required": [
        "repo"
      ],
      "title": "RepoRef",
      "type": "object"
    }
  },
  "$id": "urn:nel:contracts:v1:history-rewrite-intent",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "actor": {
      "$ref": "#/$defs/ClaimedActor"
    },
    "intent_id": {
      "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
      "title": "Intent Id",
      "type": "string"
    },
    "kind": {
      "const": "history_rewrite",
      "default": "history_rewrite",
      "title": "Kind",
      "type": "string"
    },
    "ref": {
      "title": "Ref",
      "type": "string"
    },
    "repo": {
      "$ref": "#/$defs/RepoRef"
    },
    "submitted_at": {
      "format": "date-time",
      "title": "Submitted At",
      "type": "string"
    }
  },
  "required": [
    "intent_id",
    "actor",
    "submitted_at",
    "repo",
    "ref"
  ],
  "title": "HistoryRewriteIntent",
  "type": "object"
}
