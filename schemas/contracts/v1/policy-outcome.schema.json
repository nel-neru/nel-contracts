{
  "$comment": "generated from pinned pydantic + type-level overrides, guarded by generate-and-diff (nel-contracts projector 1); a fuller hand-controlled projection layer is deferred (K-phase)",
  "$id": "urn:nel:contracts:v1:policy-outcome",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "The neutral outcome shape a policy evaluation projects. ``block`` names the gate; a\nworld-built instance is non-authoritative.",
  "properties": {
    "decided_at": {
      "format": "date-time",
      "title": "Decided At",
      "type": "string"
    },
    "gate": {
      "anyOf": [
        {
          "examples": [
            "external_publish_or_public_repo_mutation",
            "history_rewrite",
            "irreversible_architecture_decision_without_adr_precedent",
            "irreversible_destructive_migration",
            "legal_or_license_decision",
            "main_merge",
            "missing_credentials_or_permissions",
            "payment_or_purchase",
            "pii_storage_policy_change"
          ],
          "title": "GateClass",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Gate"
    },
    "outcome": {
      "enum": [
        "allow",
        "block"
      ],
      "title": "Outcome",
      "type": "string"
    },
    "reason": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Reason"
    }
  },
  "required": [
    "outcome",
    "decided_at"
  ],
  "title": "PolicyOutcome",
  "type": "object"
}
