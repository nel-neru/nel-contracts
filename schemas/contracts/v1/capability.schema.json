{
  "$comment": "generated from pinned pydantic + type-level overrides, guarded by generate-and-diff (nel-contracts projector 1); a fuller hand-controlled projection layer is deferred (K-phase)",
  "$id": "urn:nel:contracts:v1:capability",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "A neutral capability shape under a default-deny classification.\n\n``internal_only`` additionally requires a structurally verified ``egress == \"none\"``.\nAny capability whose executor touches the network or credentials is ``external``\nregardless of label (resolves the \"internal_only allowlist is not self-completing\"\nfinding).",
  "properties": {
    "capability_id": {
      "pattern": "^[a-z0-9][a-z0-9-]{0,63}$",
      "title": "Capability Id",
      "type": "string"
    },
    "classification": {
      "examples": [
        "external",
        "internal_only"
      ],
      "title": "CapabilityClassification",
      "type": "string"
    },
    "description": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Description"
    },
    "egress": {
      "default": "external",
      "enum": [
        "none",
        "kernel_channel",
        "external"
      ],
      "title": "Egress",
      "type": "string"
    }
  },
  "required": [
    "capability_id",
    "classification"
  ],
  "title": "Capability",
  "type": "object"
}
