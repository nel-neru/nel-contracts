{
  "$comment": "generated from pinned pydantic + type-level overrides, guarded by generate-and-diff (nel-contracts projector 1); a fuller hand-controlled projection layer is deferred (K-phase)",
  "$id": "urn:nel:contracts:v1:mediated-capability-grant",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "A kernel-internal, per-approval grant shape. It is never a bearer token and there is\nno ``request_capability`` port verb; construction here carries no authority.",
  "properties": {
    "capability_id": {
      "pattern": "^[a-z0-9][a-z0-9-]{0,63}$",
      "title": "Capability Id",
      "type": "string"
    },
    "expires_at": {
      "anyOf": [
        {
          "format": "date-time",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Expires At"
    },
    "single_use": {
      "default": true,
      "title": "Single Use",
      "type": "boolean"
    }
  },
  "required": [
    "capability_id"
  ],
  "title": "MediatedCapabilityGrant",
  "type": "object"
}
