{
  "$comment": "generated by nel-contracts projector 1",
  "$defs": {
    "SeamReference": {
      "additionalProperties": false,
      "description": "A pull-only reference to one intent/work_item/run/approval, scoped by authenticated\ncaller identity kernel-side.",
      "properties": {
        "ref_id": {
          "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
          "title": "Ref Id",
          "type": "string"
        },
        "ref_kind": {
          "enum": [
            "intent",
            "work_item",
            "run",
            "approval"
          ],
          "title": "Ref Kind",
          "type": "string"
        }
      },
      "required": [
        "ref_kind",
        "ref_id"
      ],
      "title": "SeamReference",
      "type": "object"
    }
  },
  "$id": "urn:nel:contracts:v1:seam-observation",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "The single read-surface projection. ``authority`` is fixed to ``projection`` — an\nobservation is never authoritative caller-side.",
  "properties": {
    "authority": {
      "const": "projection",
      "default": "projection",
      "title": "Authority",
      "type": "string"
    },
    "evidence_digests": {
      "items": {
        "pattern": "^[0-9a-f]{64}$",
        "type": "string"
      },
      "title": "Evidence Digests",
      "type": "array"
    },
    "observed_at": {
      "format": "date-time",
      "title": "Observed At",
      "type": "string"
    },
    "reference": {
      "$ref": "#/$defs/SeamReference"
    },
    "state": {
      "examples": [
        "accepted",
        "blocked",
        "pending",
        "rejected",
        "unavailable"
      ],
      "title": "SubmissionState",
      "type": "string"
    }
  },
  "required": [
    "reference",
    "state",
    "observed_at"
  ],
  "title": "SeamObservation",
  "type": "object"
}
