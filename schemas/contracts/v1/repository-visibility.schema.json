{
  "$comment": "generated by nel-contracts projector 1",
  "$defs": {
    "RepoRef": {
      "additionalProperties": false,
      "description": "A logical, portable repository reference. Carries no filesystem path (a public\ncontract must never leak a governor-local ``C:\\Users`` path).",
      "properties": {
        "host": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Host"
        },
        "repo": {
          "pattern": "^[a-z0-9][a-z0-9-]{0,63}$",
          "title": "Repo",
          "type": "string"
        }
      },
      "required": [
        "repo"
      ],
      "title": "RepoRef",
      "type": "object"
    }
  },
  "$id": "urn:nel:contracts:v1:repository-visibility",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "A digest-snapshotted and live-reconciled repository visibility.\n\nA public-release-class operation requires the snapshot to match the live value; a\nmismatch blocks (enforced kernel-side; :meth:`matches` is the shape-level check).",
  "properties": {
    "live": {
      "anyOf": [
        {
          "examples": [
            "private",
            "public"
          ],
          "title": "VisibilityLevel",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Live"
    },
    "repo": {
      "$ref": "#/$defs/RepoRef"
    },
    "snapshot": {
      "examples": [
        "private",
        "public"
      ],
      "title": "VisibilityLevel",
      "type": "string"
    },
    "snapshot_digest": {
      "anyOf": [
        {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Snapshot Digest"
    }
  },
  "required": [
    "repo",
    "snapshot"
  ],
  "title": "RepositoryVisibility",
  "type": "object"
}
